# Keep node_modules excluded (always best practice)
/node_modules

# Keep debug logs excluded
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Keep local env files excluded (for security)
.env*.local

# TypeScript build info can be excluded
*.tsbuildinfo
next-env.d.ts

# These are runtime/temporary files that shouldn't be tracked
.vercel

# .gitignore
.next/cache/